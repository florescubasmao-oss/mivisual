/* ============================================================
   MI VISUAL V432 - SEGURIDAD ATS / PETAR DIGITAL
   Módulo nuevo, aislado y de carga bajo demanda.
============================================================ */
(function(){
'use strict';
const API=window.MI_VISUAL_API_URL;let SEG={ctx:null,ats:null,petar:null,catalogo:null};
const DANOS=['ELECTROCUCION','CAIDA DE OBJETOS','CAIDA A DESNIVEL','CAIDA A NIVEL','GOLPES','CORTES','ATRAPAMIENTO','LESIONES EN MANOS','CONTACTO CON SUSTANCIAS PELIGROSAS','PROYECCION DE PARTICULAS','OTROS'];
function n(v){return String(v??'').trim()}function N(v){return n(v).toUpperCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/\s+/g,' ').trim()}function e(v){return n(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function user(){return localStorage.getItem('usuario')||''}function perfil(){return N(localStorage.getItem('perfil'))}
async function api(payload,get){
 const p=Object.assign({usuario:user()},payload||{});
 if(get&&typeof mv336ApiGet==='function')return mv336ApiGet(API,p,{intentos:2,tiempoMs:25000});
 if(get){const u=new URL(API);Object.entries(p).forEach(([k,v])=>u.searchParams.set(k,String(v)));const r=await fetch(u,{cache:'no-store'}),t=await r.text();let d;try{d=JSON.parse(t)}catch(_){throw Error('Respuesta inválida de Seguridad')};if(!d.ok)throw Error(d.error||'Error de Seguridad');return d}
 const r=await fetch(API,{method:'POST',headers:{'Content-Type':'text/plain;charset=UTF-8'},body:JSON.stringify(p),cache:'no-store'}),t=await r.text();let d;try{d=JSON.parse(t)}catch(_){throw Error('Respuesta inválida de Seguridad')};if(!d.ok)throw Error(d.error||'Error de Seguridad');return d;
}
function css(){return `<style>
.seg-wrap{max-width:1180px;margin:auto;padding:10px 10px 70px;color:#0f172a}.seg-head{background:linear-gradient(120deg,#0f766e,#2563eb);color:#fff;border-radius:18px;padding:16px;margin-bottom:10px}.seg-head h2{margin:0 0 3px}.seg-head p{margin:0;font-size:12px}.seg-card{background:#fff;border:1px solid #d9e2ec;border-radius:15px;padding:12px;margin-bottom:10px;box-shadow:0 5px 15px rgba(15,23,42,.08)}.seg-btn{border:0;border-radius:10px;padding:9px 12px;font-weight:900;cursor:pointer;background:#2563eb;color:#fff}.seg-btn.gray{background:#64748b}.seg-btn.green{background:#16a34a}.seg-btn.red{background:#dc2626}.seg-btn.orange{background:#ea580c}.seg-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:9px}.seg-note{padding:9px;border-radius:10px;background:#eff6ff;color:#1e3a8a;font-size:11px}.seg-warn{background:#fff7ed;color:#9a3412}.seg-ok{background:#ecfdf5;color:#166534}.seg-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.seg-field label{display:block;font-size:10px;font-weight:900;color:#475569;margin-bottom:3px}.seg-field input,.seg-field select,.seg-field textarea{width:100%;box-sizing:border-box;border:1px solid #b8c5d3;border-radius:8px;padding:8px;background:#fff}.seg-field textarea{min-height:70px}.seg-paper{background:#fff;border:2px solid #475569;border-radius:3px;padding:8px;overflow:auto}.seg-doc-head{display:grid;grid-template-columns:170px 1fr 150px;border:1px solid #64748b;min-width:760px}.seg-doc-head>div{border-right:1px solid #64748b;padding:8px;display:flex;align-items:center;justify-content:center;text-align:center;font-weight:900}.seg-doc-head>div:last-child{border:0}.seg-doc-head img{max-width:135px;max-height:55px}.seg-doc-title{font-size:16px}.seg-nro{color:#dc2626;font-size:15px}.seg-meta{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:9px 0;min-width:760px}.seg-table{width:100%;border-collapse:collapse;min-width:900px;font-size:10px}.seg-table th,.seg-table td{border:1px solid #64748b;padding:5px;vertical-align:middle}.seg-table th{background:#f1f5f9}.seg-table input,.seg-table select,.seg-table textarea{width:100%;box-sizing:border-box;border:0;background:transparent;font:inherit}.seg-checks{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:5px}.seg-check{border:1px solid #cbd5e1;border-radius:8px;padding:6px;font-size:10px;display:flex;gap:5px;align-items:center}.seg-firma-canvas{width:100%;height:180px;border:2px dashed #94a3b8;border-radius:10px;background:#fff;touch-action:none}.seg-pend{display:grid;gap:7px}.seg-item{border:1px solid #cbd5e1;border-radius:11px;padding:9px;background:#f8fafc}.seg-pill{display:inline-block;padding:4px 7px;border-radius:999px;font-size:9px;font-weight:900;background:#e2e8f0}.seg-pill.pend{background:#fef3c7;color:#92400e}.seg-pill.ok{background:#dcfce7;color:#166534}.seg-pill.bad{background:#fee2e2;color:#991b1b}.seg-danger{padding:10px;border:2px solid #dc2626;border-radius:10px;background:#fef2f2;color:#991b1b;font-weight:900;text-align:center}.seg-task-select{min-width:160px}.seg-danos{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:2px}.seg-danos label{font-size:8px;display:flex;gap:3px}.seg-tools{display:flex;gap:6px;flex-wrap:wrap}.seg-sign{max-height:50px;max-width:140px}.seg-coll{border:1px solid #dbe3ee;border-radius:12px;overflow:hidden;margin-top:8px}.seg-coll summary{cursor:pointer;padding:10px;font-weight:900;background:#f8fafc}.seg-coll-body{padding:10px}.seg-upload{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.seg-photo{border:1px dashed #94a3b8;padding:7px;border-radius:9px;font-size:10px}
@media(max-width:700px){.seg-grid{grid-template-columns:1fr}.seg-wrap{padding:6px 6px 70px}.seg-doc-head,.seg-meta{min-width:690px}.seg-upload{grid-template-columns:1fr}.seg-paper{padding:5px}.seg-doc-title{font-size:13px}}
</style>`}
function btnVolver(){return `<button class="seg-btn gray" onclick="volverInicio()">← Volver</button>`}
function firmaHtml(f){return f&&f.url?`<img class="seg-sign" src="${e(f.url)}" alt="Firma">`:'<span class="seg-pill bad">SIN FIRMA</span>'}
function home(){
 const c=SEG.ctx||{}, p=perfil(), tec=p==='TECNICO';
 let html=css()+`<div class="seg-wrap"><div class="seg-head"><h2>🦺 Seguridad · ATS / PETAR</h2><p>Documentos preventivos digitales con trazabilidad, firmas y validación.</p><div class="seg-actions">${btnVolver()}</div></div>`;
 const firmaEstado=c.firma?.activa?`<span class="seg-pill ok">REGISTRADA · V${e(c.firma.version)}</span>`:`<span class="seg-pill bad">PENDIENTE</span>`;
 const firmaBoton=c.firma?.activa?`<button class="seg-btn gray" onclick="seg432SolicitarCambioFirma()">Solicitar cambio de firma</button>`:`<button class="seg-btn" onclick="seg432Firma()">✍️ Registrar firma</button>`;
 html+=`<div class="seg-card"><b>Firma digital</b><div style="margin-top:7px">${firmaEstado}</div><div class="seg-actions">${firmaBoton}</div></div>`;
 if(tec){
   const hay=!!c.hoy?.ats;
   html+=`<div class="seg-card"><b>ATS / PETAR del día</b><div class="seg-note" style="margin-top:7px">${hay?`ATS N° ${e(c.hoy.ats.numero)} · ${e(c.hoy.ats.estado)}`:'Aún no se ha generado el ATS de hoy para la cuadrilla.'}</div><div class="seg-actions">${hay?`<button class="seg-btn green" onclick="seg432AbrirAts('${e(c.hoy.ats.id)}')">Abrir ATS/PETAR</button>`:`<button class="seg-btn green" onclick="seg432GenerarDia(this)">Generar ATS/PETAR del día</button>`}</div></div>`;
 }else{
   const pend=(c.pendientes||[]);
   const pendHtml=pend.length?pend.map(x=>`<div class="seg-item"><b>ATS N° ${e(x.numero)} · ${e(x.cuadrilla)}</b><small style="display:block">${e(x.fecha)} · ${e(x.sede)} · ${e(x.estado)}</small><button class="seg-btn" style="margin-top:6px" onclick="seg432AbrirAts('${e(x.id)}')">Revisar</button></div>`).join(''):'<div class="seg-note seg-ok">Sin pendientes.</div>';
   html+=`<div class="seg-card"><b>Pendientes de revisión</b><div id="segPend" class="seg-pend" style="margin-top:8px">${pendHtml}</div></div>`;
   const sols=(c.solicitudesFirma||[]);
   if(sols.length){
     html+=`<div class="seg-card"><b>Solicitudes de cambio de firma</b><div class="seg-pend" style="margin-top:8px">${sols.map(x=>`<div class="seg-item"><b>${e(x.nombre)}</b><small style="display:block">${e(x.usuario)} · ${e(x.motivo)}</small><div class="seg-actions"><button class="seg-btn green" onclick="seg432ResolverFirma('${e(x.id)}','APROBADO')">Autorizar</button><button class="seg-btn red" onclick="seg432ResolverFirma('${e(x.id)}','RECHAZADO')">Rechazar</button></div></div>`).join('')}</div></div>`;
   }
 }
 html+='</div>'; mostrarPantalla(html);
}
async function cargar(){const d=await api({accion:'obtenerContextoSeguridadV432'},true);SEG.ctx=d;SEG.catalogo=d.catalogo||{};home()}
async function mostrarSeguridad(){limpiarPantalla();document.getElementById('menuPrincipal')?.style.setProperty('display','none','important');setBotonNavegacion('modulo');mostrarPantalla(css()+`<div class="seg-wrap"><div class="seg-card">Cargando Seguridad...</div></div>`);try{await cargar()}catch(err){mostrarPantalla(css()+`<div class="seg-wrap"><div class="seg-card seg-warn">${e(err.message)}</div>${btnVolver()}</div>`)}}
async function generarDia(btn){if(btn)btn.disabled=true;try{const g=await geo();await api({accion:'crearAtsDiaSeguridadV432',gps:g});await cargar()}catch(err){alert(err.message)}finally{if(btn)btn.disabled=false}}
function geo(){return new Promise(r=>{if(!navigator.geolocation)return r('');navigator.geolocation.getCurrentPosition(p=>r(`${p.coords.latitude.toFixed(6)},${p.coords.longitude.toFixed(6)}`),()=>r(''),{enableHighAccuracy:true,timeout:8000,maximumAge:30000})})}
async function abrirAts(id){try{const d=await api({accion:'obtenerDocumentoSeguridadV432',id},true);SEG.ats=d.ats;SEG.petar=d.petar;renderAts()}catch(err){alert(err.message)}}
function taskRow(t,i,editable){const opts=(SEG.catalogo?.tareas||[]).map(x=>`<option ${N(x.tarea)===N(t.tarea)?'selected':''}>${e(x.tarea)}</option>`).join('');return `<tr data-task="${i}"><td><select class="seg-task-select" ${editable?'':'disabled'} onchange="seg432TareaCambio(${i},this.value)">${opts}<option ${!((SEG.catalogo?.tareas||[]).some(x=>N(x.tarea)===N(t.tarea)))?'selected':''}>OTRA</option></select>${editable?`<input data-k="tarea" value="${e(t.tarea||'')}" placeholder="Detalle de tarea">`:`<div>${e(t.tarea)}</div>`}</td><td><div class="seg-danos">${DANOS.map(d=>`<label><input type="checkbox" data-d="${e(d)}" ${t.danos?.includes(d)?'checked':''} ${editable?'':'disabled'}> ${e(d)}</label>`).join('')}</div></td><td><textarea data-k="controles" ${editable?'':'readonly'}>${e(t.controles||'')}</textarea></td></tr>`}
function renderAts(){const a=SEG.ats,c=SEG.ctx||{},editable=perfil()==='TECNICO'&&['BORRADOR','OBSERVADO'].includes(N(a.estado));const acept=a.aceptaciones||[];let h=css()+`<div class="seg-wrap"><div class="seg-actions">${btnVolver()}<button class="seg-btn gray" onclick="seg432CargarHome()">Inicio Seguridad</button></div><div class="seg-paper" style="margin-top:8px"><div class="seg-doc-head"><div><img src="./img/logo.png"></div><div class="seg-doc-title">ANÁLISIS DE TRABAJO SEGURO (ATS)</div><div><div>ATS N° <span class="seg-nro">${e(a.numero)}</span><br><small>SGSST_ATS_01<br>Versión: ${e(a.version||1)}</small></div></div></div><div class="seg-meta"><div class="seg-grid"><div class="seg-field"><label>TRABAJO A REALIZAR</label><input id="segTrabajo" value="${e(a.trabajo)}" ${editable?'':'readonly'}></div><div class="seg-field"><label>FECHA</label><input value="${e(a.fecha)}" readonly></div><div class="seg-field"><label>HORA DE INICIO</label><input value="${e(a.horaInicio)}" readonly></div></div><div class="seg-grid"><div class="seg-field"><label>PERMISO PETAR N°</label><input value="${e(SEG.petar?.numero||'')}" readonly></div><div class="seg-field"><label>LUGAR DE TRABAJO</label><input id="segLugar" value="${e(a.lugarTrabajo)}" ${editable?'':'readonly'}></div><div class="seg-field"><label>HORA FINAL</label><input id="segHoraFinal" value="${e(a.horaFinal||'')}" ${editable?'':'readonly'}></div></div></div>`;
 h+=`<table class="seg-table"><tr><th colspan="4">EQUIPOS DE PROTECCIÓN PERSONAL</th><th colspan="2">HERRAMIENTAS Y EQUIPOS</th></tr><tr><td colspan="4"><div class="seg-checks">${(SEG.catalogo?.epp||[]).map(x=>`<label class="seg-check"><input type="checkbox" data-epp="${e(x)}" ${(a.epp||[]).includes(x)?'checked':''} ${editable?'':'disabled'}>${e(x)}</label>`).join('')}</div></td><td colspan="2"><textarea id="segHerr" ${editable?'':'readonly'}>${e(a.herramientas||'')}</textarea></td></tr></table>`;
 h+=`<table class="seg-table" id="segTaskTable"><thead><tr><th style="width:22%">LISTA DE TAREAS</th><th style="width:45%">POSIBILIDAD DE DAÑO</th><th>MEDIDAS DE CONTROL</th></tr></thead><tbody>${(a.tareas||[]).map((t,i)=>taskRow(t,i,editable)).join('')}</tbody></table>${editable?`<div class="seg-actions"><button class="seg-btn" onclick="seg432AgregarTarea()">+ Agregar tarea</button></div>`:''}`;
 h+=`<table class="seg-table" style="margin-top:8px"><tr><th colspan="3">HE LEÍDO Y ENTENDIDO ESTE DOCUMENTO (cada miembro de la cuadrilla debe aceptar)</th></tr>${(a.integrantes||[]).map(x=>{const ac=acept.find(y=>N(y.usuario)===N(x.usuario));return `<tr><td>${e(x.nombre)}</td><td>${e(x.cargo)}</td><td>${ac?firmaHtml(ac.firma):'<span class="seg-pill pend">PENDIENTE</span>'}</td></tr>`}).join('')}<tr><td colspan="2">Supervisor / Responsable</td><td>${a.supervisorFirma?firmaHtml(a.supervisorFirma):'<span class="seg-pill pend">PENDIENTE</span>'}</td></tr></table></div>`;
 if(SEG.petar)h+=`<details class="seg-coll" open><summary>PETAR - ALTURA N° ${e(SEG.petar.numero)}</summary><div class="seg-coll-body" id="segPetarArea"></div></details>`;
 h+=`<div class="seg-card"><b>Estado: ${e(a.estado)}</b>${editable?`<div class="seg-actions"><button class="seg-btn" onclick="seg432Guardar()">Guardar borrador</button><button class="seg-btn green" onclick="seg432Aceptar()">Aceptar y firmar</button></div>`:''}${!editable&&perfil()==='TECNICO'&&!acept.some(x=>N(x.usuario)===N(user()))&&['PENDIENTE_ACEPTACION','BORRADOR'].includes(N(a.estado))?`<div class="seg-actions"><button class="seg-btn green" onclick="seg432Aceptar()">Aceptar y firmar</button></div>`:''}${perfil()==='SUPERVISOR'&&N(a.estado)==='PENDIENTE SUPERVISOR'?`<div class="seg-actions"><button class="seg-btn green" onclick="seg432Revisar('AUTORIZAR')">Autorizar con firma</button><button class="seg-btn orange" onclick="seg432Revisar('OBSERVAR')">Observar</button><button class="seg-btn red" onclick="seg432Revisar('RECHAZAR')">Rechazar</button></div>`:''}${['JEFATURA','JEFATURA GENERAL','JEFATURA OPERACIONES','JEFATURA DE OPERACIONES','GERENCIA LIMA','GERENCIA GENERAL','GERENCIAL GENERAL','ADMIN','ADMINISTRADOR'].includes(perfil())&&N(a.estado)==='PENDIENTE VALIDACION'?`<div class="seg-actions"><button class="seg-btn green" onclick="seg432ValidarFinal('VALIDAR')">Validar y cerrar</button><button class="seg-btn orange" onclick="seg432ValidarFinal('OBSERVAR')">Observar</button><button class="seg-btn red" onclick="seg432ValidarFinal('RECHAZAR')">Rechazar</button></div>`:''}${a.pdfUrl?`<div class="seg-actions"><a class="seg-btn green" target="_blank" href="${e(a.pdfUrl)}">PDF ATS</a>${SEG.petar?.pdfUrl?`<a class="seg-btn green" target="_blank" href="${e(SEG.petar.pdfUrl)}">PDF PETAR</a>`:''}</div>`:''}</div></div>`;mostrarPantalla(h);if(SEG.petar)renderPetar(editable)}
function renderPetar(editable){const p=SEG.petar,el=document.getElementById('segPetarArea');if(!el)return;const critical=p.noCumpleCritico;el.innerHTML=`${critical?'<div class="seg-danger">🔴 TRABAJO NO AUTORIZADO</div>':''}<div class="seg-paper"><div class="seg-doc-head"><div><img src="./img/logo.png"></div><div class="seg-doc-title">PERMISO ESCRITO PARA TRABAJOS DE ALTO RIESGO (PETAR) - ALTURA</div><div>PETAR N° <span class="seg-nro">${e(p.numero)}</span></div></div><div class="seg-meta"><div>TRABAJO: ${e(p.trabajo)}</div><div>FECHA: ${e(p.fecha)}<br>HORA INICIO: ${e(p.horaInicio)}<br>HORA FINAL: ${e(p.horaFinal||'')}</div><div>UBICACIÓN: ${e(p.ubicacion)}</div></div><table class="seg-table"><thead><tr><th>#</th><th>LISTA DE VERIFICACIÓN</th><th>VERIFICACIÓN</th><th>OBSERVACIONES</th></tr></thead><tbody>${(p.checklist||[]).map((x,i)=>`<tr><td>${i+1}</td><td>${e(x.texto)}</td><td><select data-petar-i="${i}" ${editable?'':'disabled'}><option></option>${['CUMPLE','NO CUMPLE','NO APLICA'].map(v=>`<option ${N(x.estado)===v?'selected':''}>${v}</option>`).join('')}</select></td><td><input data-petar-o="${i}" value="${e(x.observacion||'')}" ${editable?'':'readonly'}></td></tr>`).join('')}</tbody></table><div style="margin-top:8px"><b>EQUIPO DE PROTECCIÓN REQUERIDO</b><div class="seg-checks">${(SEG.catalogo?.eppPetar||[]).map(x=>`<label class="seg-check"><input type="checkbox" data-pepp="${e(x)}" ${(p.epp||[]).includes(x)?'checked':''} ${editable?'':'disabled'}>${e(x)}</label>`).join('')}</div></div></div>`}
function datosAts(){const tareas=[...document.querySelectorAll('#segTaskTable tbody tr')].map(tr=>({tarea:n(tr.querySelector('[data-k="tarea"]')?.value||tr.querySelector('.seg-task-select')?.value),danos:[...tr.querySelectorAll('[data-d]:checked')].map(x=>x.dataset.d),controles:n(tr.querySelector('[data-k="controles"]')?.value)})).filter(x=>x.tarea);return {trabajo:n(document.getElementById('segTrabajo')?.value),lugarTrabajo:n(document.getElementById('segLugar')?.value),horaFinal:n(document.getElementById('segHoraFinal')?.value),herramientas:n(document.getElementById('segHerr')?.value),epp:[...document.querySelectorAll('[data-epp]:checked')].map(x=>x.dataset.epp),tareas}}
function datosPetar(){if(!SEG.petar)return null;return {checklist:(SEG.petar.checklist||[]).map((x,i)=>Object.assign({},x,{estado:n(document.querySelector(`[data-petar-i="${i}"]`)?.value),observacion:n(document.querySelector(`[data-petar-o="${i}"]`)?.value)})),epp:[...document.querySelectorAll('[data-pepp]:checked')].map(x=>x.dataset.pepp)}}
async function guardar(){try{const d=datosAts(),p=datosPetar();await api({accion:'guardarAtsSeguridadV432',id:SEG.ats.id,...d,petar:p});await abrirAts(SEG.ats.id)}catch(err){alert(err.message)}}
function agregarTarea(){SEG.ats.tareas=SEG.ats.tareas||[];SEG.ats.tareas.push({tarea:'OTRA',danos:[],controles:''});renderAts()}
function tareaCambio(i,val){const cat=(SEG.catalogo?.tareas||[]).find(x=>N(x.tarea)===N(val));if(!cat)return;SEG.ats.tareas[i]={tarea:cat.tarea,danos:[...(cat.danos||[])],controles:(cat.controles||[]).join('\n')};renderAts()}
async function aceptar(){try{await guardar();const g=await geo();await api({accion:'aceptarAtsSeguridadV432',id:SEG.ats.id,gps:g});await abrirAts(SEG.ats.id)}catch(err){alert(err.message)}}
async function revisar(accion){const motivo=accion==='AUTORIZAR'?'':prompt('Motivo:')||'';if(accion!=='AUTORIZAR'&&!motivo)return;try{await api({accion:'revisarAtsSupervisorV432',id:SEG.ats.id,resultado:accion,motivo});await abrirAts(SEG.ats.id)}catch(err){alert(err.message)}}
async function validarFinal(resultado){const motivo=resultado==='VALIDAR'?'':prompt('Motivo:')||'';if(resultado!=='VALIDAR'&&!motivo)return;try{const d=await api({accion:'validarAtsFinalV432',id:SEG.ats.id,resultado,motivo});if(d.pdfUrl)alert('Documento cerrado y PDF generado.');await abrirAts(SEG.ats.id)}catch(err){alert(err.message)}}
function firma(){mostrarPantalla(css()+`<div class="seg-wrap"><div class="seg-card"><h3>Registrar firma digital</h3><div class="seg-field"><label>DNI</label><input id="segDni" inputmode="numeric" maxlength="12"></div><canvas id="segCanvas" class="seg-firma-canvas" width="900" height="260"></canvas><div class="seg-actions"><button class="seg-btn gray" onclick="seg432LimpiarFirma()">Limpiar</button><button class="seg-btn green" onclick="seg432GuardarFirma()">Guardar firma</button><button class="seg-btn gray" onclick="seg432CargarHome()">Cancelar</button></div><div class="seg-note">La firma se registra una sola vez. Para cambiarla posteriormente se requiere autorización.</div></div></div>`);setTimeout(initCanvas,0)}
function initCanvas(){const c=document.getElementById('segCanvas');if(!c)return;const x=c.getContext('2d');x.lineWidth=4;x.lineCap='round';x.strokeStyle='#0f172a';let d=false;const pos=ev=>{const r=c.getBoundingClientRect(),p=ev.touches?ev.touches[0]:ev;return {x:(p.clientX-r.left)*c.width/r.width,y:(p.clientY-r.top)*c.height/r.height}};const start=ev=>{d=true;const p=pos(ev);x.beginPath();x.moveTo(p.x,p.y);ev.preventDefault()};const move=ev=>{if(!d)return;const p=pos(ev);x.lineTo(p.x,p.y);x.stroke();ev.preventDefault()};const stop=()=>d=false;c.addEventListener('pointerdown',start);c.addEventListener('pointermove',move);window.addEventListener('pointerup',stop)}
function limpiarFirma(){const c=document.getElementById('segCanvas');c?.getContext('2d').clearRect(0,0,c.width,c.height)}
async function guardarFirma(){const c=document.getElementById('segCanvas'),dni=n(document.getElementById('segDni')?.value);if(dni.length<8)return alert('Ingrese DNI válido.');try{const g=await geo();await api({accion:'registrarFirmaSeguridadV432',dni,gps:g,firmaBase64:c.toDataURL('image/png').split(',')[1]});await cargar()}catch(err){alert(err.message)}}
async function solicitarCambioFirma(){const motivo=prompt('Indique el motivo del cambio de firma:');if(!motivo)return;try{await api({accion:'solicitarCambioFirmaSeguridadV432',motivo});alert('Solicitud enviada.');await cargar()}catch(err){alert(err.message)}}
async function resolverFirma(id,resultado){try{await api({accion:'resolverCambioFirmaSeguridadV432',id,resultado});await cargar()}catch(err){alert(err.message)}}
window.mostrarSeguridad=mostrarSeguridad;window.seg432CargarHome=cargar;window.seg432GenerarDia=generarDia;window.seg432AbrirAts=abrirAts;window.seg432Guardar=guardar;window.seg432AgregarTarea=agregarTarea;window.seg432TareaCambio=tareaCambio;window.seg432Aceptar=aceptar;window.seg432Revisar=revisar;window.seg432ValidarFinal=validarFinal;window.seg432Firma=firma;window.seg432LimpiarFirma=limpiarFirma;window.seg432GuardarFirma=guardarFirma;window.seg432SolicitarCambioFirma=solicitarCambioFirma;window.seg432ResolverFirma=resolverFirma;
})();